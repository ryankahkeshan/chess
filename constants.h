#ifndef __CONSTANTS_H__
#define __CONSTANTS_H__
const int BOARD_WIDTH = 8;
const int BOARD_HEIGHT = 8;

const int DRAW_GAME = 50; // Automatic draw after number of no-capture moves in a row

const int GRAPHICAL_DISPLAY_HEIGHT = 720;
const int GRAPHICAL_DISPLAY_WIDTH = 720;
const int SQUARE_SIZE = 80;

#endif
